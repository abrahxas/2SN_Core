{% extends 'CoreGalleryBundle:default:show.html.twig' %}
{% block photo %}

{% for photo in photos %}
    <div style="width: 20%; background-color: #0088CC;padding: 1%;margin-bottom: 2%">
        <div style="background-color: white">
            <a href="{{ path('core_photo_update', {'albumId': albumId,'id': photo.id}) }}">update</a>
            <a href="{{ path('core_photo_delete', {'albumId': albumId,'id': photo.id}) }}">delete</a>
            <br>
            <br>
            <a href="{{ path('core_photo_show', {'albumId': albumId,'id': photo.id}) }}">{{ photo.title }}</a>
            <br>
            <br>
            {{ photo.content }}
        </div>
    </div>
{% endfor %}

{% endblock photo %}